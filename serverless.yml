service: Lambda-fun-deploy

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: ap-south-1

functions:
  Front:
    handler: src/handler.hello

  Login:
    handler: src1/handler.handler
    
plugins:
    - serverless-webpack 

package:
    individually: true